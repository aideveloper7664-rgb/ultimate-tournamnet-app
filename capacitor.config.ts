import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.tournament',
  appName: 'Ultimate Tournament',
  webDir: 'dist'
};

export default config;
